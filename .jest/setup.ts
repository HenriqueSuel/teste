import '@testing-library/jest-dom'
// jest.mock('next/navigation', () => require('next-router-mock'));

/* jest.mock('next/navigation', () => ({
    useRouter: jest.fn(),
})); */

jest.mock('@/context/github.context');